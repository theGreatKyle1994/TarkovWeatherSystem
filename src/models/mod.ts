export interface ModConfig {
  enable: boolean;
  enableSeasons: boolean;
  enableWeather: boolean;
}

export const modConfigDefaults = {
  enable: true,
  enableSeasons: true,
  enableWeather: true,
};
